# AutoTrade KR — 전수 코드 리뷰 리포트
### 2026-06-04 · 새 환경 이전 후 사전 서비스 감사 · 11,712줄 전수 검토

## 0. 환경 검증

- Node v22.22.0, 문법 체크 6개 모듈 전부 통과
- 자동화 테스트 **68/68 통과** (engine 14 / realtime 20 / fallback 21 / journal 13)
- HTML 구조: div 552/552 균형, 중복 ID 없음 — 이전 `</div>` 수정 유효

> 테스트는 다 통과하지만, 아래 CRITICAAL 이슈들은 테스트가 커버하지 않는 영역에서 발견됨.

---

## 1. CRITICAL — 배포 전 반드시 수정 (전부 코드에서 직접 검증함)

### ① 로그인 바이패스가 운영 코드에 남아 있음 — `app.html:3790`
```js
// ★[임시] 로그인 화면 주석 처리(개발용 바이패스). 운영 전 반드시 원복 ★
// if (!me) { ... return; }
$('app').style.display = 'grid';   // 무인증 진입
```
인증 없이 누구나 주문·취소·자동매매 시작 가능. **주석 4줄 원복.**

### ② `_currentUserId` 전역 변수 경합 → 타 유저 계좌로 주문 가능 — `proxy-server.js:20-21, 55, 67`
요청 핸들러가 async인데 유저 식별을 전역 변수 하나로 함. A 요청이 `await` 중에 B 요청이 들어오면 A의 주문/설정 저장이 **B의 계좌·설정 파일로** 들어감.