# 적대적 전수조사 리포트 — 2026-07-30

> 대상: AutoTrade KR 전 기능·전 차트
> 방법: 7개 영역 병렬 적대 감사(탐지) → 영역별 독립 반박 검증. 검증 통과분만 수록.
> 결과: **확정/불확실 49건** (반박으로 제외 2건). 심각도는 검증관이 조정한 최종값 기준.
> 커버리지: proxy-server.js · auto-trader.js · kis-realtime.js · auth.js · order-journal.js · app.html(기능·차트 렌더링·지표 수학)

## 처리 현황 (2026-07-30)

**HIGH 2 + MEDIUM 6 전부 수정 완료.** 테스트 218→226개(+8) 전부 통과.

| ID | 상태 | 요지 |
|---|---|---|
| pos-1 | ✅ 수정 | 저널 봇 실체결량으로 매도 상한 캡 — 수동/미체결분 오매도 차단 (테스트 +3) |
| macd-1 | ✅ 수정 | `e12.slice(14)` + maxV 비유한값 방어 — 전 종목 MACD 패널 복구 |
| reconcile-1 | ✅ 수정 | 미체결 조회 실패 틱엔 봇 지분 대조 보류 (테스트 +1) |
| loss-1 | ✅ 수정 | 시장가 손절에 슬리피지 버퍼 — 손실 서킷 보수적 발동 |
| watchdog-1 | ✅ 수정 | `_lastData`로 무데이터 동결 별도 감지 (테스트 +2) |
| render-4 | ✅ 수정 | loadTradingChart seq 검사 — 종목↔차트 불일치 차단 |
| lru-1 | ✅ 수정 | 참조카운트 축출 — 멀티탭 활성 구독 상호 보존 (테스트 +2) |
| reconnect-1 | ✅ 수정 | 재연결 지터 + approval_key 6h 캐시 — 폭주 분산 |

**추가 실사용 버그(감사 외) 수정:**
| 항목 | 상태 | 요지 |
|---|---|---|
| **현재가↔차트 불일치** | ✅ 수정 | 실시간 현재가로 일봉 '오늘 봉'을 갱신(applyLivePrice) — 헤더 현재가와 차트 현재가선이 장중에 벌어지던 문제(개발 초기부터). 대시보드 일봉 한정, 오늘 봉일 때만 |

**LOW 배치 수정 (2차, 8건):**
| 항목 | 상태 | 요지 |
|---|---|---|
| render-1 | ✅ | parseChart가 결측 OHLC(0) 봉 필터 — 스케일 붕괴 방지 |
| render-2 | ✅ | BB 상/하단을 min/max 범위에 포함 — 플롯 밖 넘침 방지 |
| render-3 | ✅ | renderChart 진입부 W/H==0 가드 |
| render-8 | ✅ | 무변동(min==max) 시 ±0.5% 여백으로 중앙 배치 |
| misc-2 | ✅ | ord_dvsn_name esc() (주문/거래내역 2곳) |
| auth-2 | ✅ | decrypt 실패 로깅(무음 자격증명 소실 관측) |
| render-7 | ✅ | 메인 차트 터치 팬 + 핀치 줌(모바일) |
| proxy-4 | ✅ | 분봉 캐시 TTL 프루닝(무한 팽창 방지) |

테스트 218→226. 남은 LOW/불확실 33건은 아래 목록 참고(미착수 — 성능·표시 미세·불확실).

## 심각도 요약 (조정 후)

| 심각도 | 건수 | 대표 |
|---|---|---|
| HIGH | 2 | 봇이 수동 보유분 매도(pos-1) · MACD 패널 상시 깨짐(macd-1) |
| MEDIUM | 6 | 손절 손익 과소계상 · 봇지분 삭제 무손절 · WS 동결 미감지 · 구독 스래싱 · 재연결 폭주 · 차트 종목불일치 |
| LOW/불확실 | 41 | 견고성·표시·성능·서비스확장 대비 |

반박으로 제외(원래 HIGH/MEDIUM이었으나 방어 확인됨): `proxy-1`(kisPost 동시주문 상호배제 — 엔진/취소 루프가 전부 `await` 직렬이라 무근거), `auth-1`(userConfigPath 교차유출 — 모든 호출부가 truthy userId 강제).

---

## HIGH — 우선 수정 권장

### pos-1 · 봇이 수동 보유분을 매도할 수 있음 (protectManual 무력화)
`auto-trader.js:1023-1038, 688-714, 990-994`
buy()가 `rt_cd==='0'`(접수)만 보고 즉시 `botPositions[code].qty`를 주문수량으로 기록. 지정가 매수가 미체결/부분체결로 남으면 봇 지분이 유령 수량으로 부풀려진다. 잔고 대조는 `expected=realQty(holdMap)+pendBuyQty`로 축소를 판정하는데 holdMap이 수동/봇 체결분을 구분 못 해, 사용자가 같은 종목을 수동 보유 중이면 realQty가 커서 봇 지분이 절대 축소되지 않는다. 이후 SELL 신호에서 `_sellableQty=min(botQty,heldQty)`가 그 부풀린 수량만큼 매도를 허용 → **수동 보유 주식을 봇이 판다.** protectManual이 막으려던 바로 그 사고(수동 삼성전자를 RSI 과매수로 매도)의 재현. 봇 watchList가 대형우량주라 수동 보유와 겹칠 확률이 높아 전제조건이 현실적.
- 수정: botPositions.qty를 접수가 아닌 **잔고 대조(실제 체결)** 시점에만 증가시키거나, 접수분을 `_buyPending` 별도 필드로 두고 sellable 계산에서 제외. reconcile 시 저널 기반 봇 체결분(주문별 fillQty)으로 수동분과 분리.

### macd-1 · MACD 패널이 모든 입력에서 깨짐 (off-by-one + trailing NaN)
`app.html:2273-2279`
ema()가 `r[0]=SMA(첫 p개)`로 시드하므로 두 EMA를 같은 봉에 정렬하려면 `e12.slice(14)`가 맞는데 코드는 `e12.slice(13)`. 두 가지 결과: (a) MACD가 EMA12를 1봉 지연시켜 계산(off-by-one), (b) `e12.slice(13)` 길이가 e26보다 1 길어 map 마지막에서 `e26[undefined]` → **macdLine 마지막값이 항상 NaN**. 그 NaN이 `allV`에 섞여 `Math.max(...)=NaN`, `maxV=NaN||1=1`이 되고, 원화 종목 MACD 실제 크기(수백~수천)가 스케일 1에 매핑돼 전부 캔버스 밖으로 나간다 → **MACD 지표를 켠 모든 차트에서 패널이 사실상 빈/평탄선으로 표시되고 오늘 봉은 아예 안 그려짐.** 매매/자금엔 영향 없는 순수 표시 결함이나 항상 발생.
- 수정: `e12.slice(13)` → `e12.slice(14)` (한 글자). off-by-one과 NaN·스케일 붕괴가 동시 해결. 방어적으로 maxV 계산에서 비유한값 제외 권장.

---

## MEDIUM

- **loss-1** `auto-trader.js:697,703-704` — 시장가 손절의 실현손익을 실제 체결가가 아닌 신호시점 라이브가(lastSellPrice)로 계상. 라이브 조회 실패 시 최대 5분 묵은 curPrice로 폴백돼 과소계상 → 갭·급락에서 dailyLossLimit 서킷이 늦게/덜 걸려 실손실이 한도 초과 가능. 수정: 실현손익 basis를 잔고 대조의 실제 체결가(현금델타/avgPrice 변화)로 산출.
- **reconcile-1** `auto-trader.js:688-714,722-730` — 봇 매수 접수 직후 미체결 상태에서 `getPendingOrders()`가 예외/빈배열이면 pendBuyQty=0 → expected<botQty로 봇 지분이 삭제(delete)된다. 직후 실제 체결되면 그 보유분이 botPositions에 없어 손절/트레일 관리에서 영구 제외 → **무기한 무손절.** 빈잔고 유예 로직 같은 가드가 이 경로엔 없는 단일점 실패. 수정: pending 조회 실패와 정상 빈목록을 구분해 실패 틱에선 축소/삭제 보류.
- **watchdog-1** `kis-realtime.js:335,371-378` — onmessage가 PINGPONG 포함 모든 프레임에 `_lastRecv` 갱신 → watchdog는 '데이터 수신'이 아닌 '아무 프레임 수신'을 측정. 재연결 후 재구독이 조용히 거부돼 시세 0건이어도 PINGPONG만 오면 90초 무수신이 영영 안 걸린다 → connected=true인 채 **시세 영구 동결.** 수정: 데이터성 수신(`_lastData`)과 keepalive를 분리해 활성 구독이 있는 동안 무데이터로 판정.
- **lru-1** `kis-realtime.js:394-412,511-515,570` — 구독 LRU가 appKey(유저) 단위 공유. 같은 유저가 두 탭/기기에서 서로 다른 30여 종목을 보면 합집합>40으로 서로의 활성 구독을 축출(스래싱), subscribe/unsubscribe가 초당 수십 건 오가며 양쪽 실시간이 깜빡인다. 수정: 참조카운트로 '어느 클라이언트도 안 보는 구독'만 축출, 활성 합집합은 보호.
- **reconnect-1** `kis-realtime.js:342,349,357-366` — 재연결 백오프에 지터 없음 + getApprovalKey 캐시 없음. 다수 피드가 KIS 순단으로 동시에 끊기면 동일 2^n 경계에 정렬돼 재연결·토큰발급 폭주(thundering herd). 다중 사용자 출시 대비 필요. 수정: `delay*(0.5+random*0.5)` 지터 + approval_key appKey별 캐시.
- **render-4** `app.html:3128-3134` — loadTradingChart가 `await` 이후 tradingLoadSeq를 검사하지 않아, 거래 페이지에서 종목 A→B 빠른 전환 시 늦게 온 A 응답이 미니차트를 덮어써 **라벨은 B인데 캔들은 A** 표시(금융앱 오인). 메인 차트(loadChart)는 `_chartReqSeq`로 막지만 미니는 무방비. 수정: `const seq=++tradingLoadSeq; ...; if(seq!==tradingLoadSeq) return;`.

---

## LOW / 불확실 (영역별 요약)

**proxy-server.js (6, 전부 LOW)**
- proxy-2 clientIp XFF 무검증 신뢰 — HOST=0.0.0.0 직접노출 시만 레이트리밋 우회(운영 기본 Tailscale+127.0.0.1은 안전)
- proxy-3 getKisTokenReal 토큰 미러/영속 부재 → 불필요 재발급(비효율)
- proxy-4 /api/chart·/api/minchart 조합 열거로 캐시·타이머 무한 팽창(_minChartCache 프루닝 없음)
- proxy-6 refreshStockinfo/Orderbook이 price 0·빈 호가도 캐시 저장(좁은 엣지)
- proxy-7 cancelStaleOrders 재진입 가드 없음(대량 미체결 시 중첩)
- proxy-8 전역 _priceCache에 무키 유저 네이버값이 키유저 표시값에 혼입(표시 한정)

**auto-trader.js (4, LOW)**
- stop-1 15:20~15:30 손절 관리 중단(스윙 전략상 의도적, 순수 공백은 10분)
- stop-2 현재가 0(거래정지 등)이면 손절 판정 스킵(허위손절 방지 목적이나 폴백 부재)
- time-1 음력 공휴일 ±1일·미등록 연도 오판(KIS 거부가 2차 게이트)
- latch-1 start()가 stoppedByLoss 래치 리셋(첫 틱 재래치로 실피해 미미)
- latency-1 긴 스캔 틱 초과 시 손절 점검 주기 지연(관리는 틱 최상단 우선실행)

**kis-realtime.js (5, LOW/불확실)**
- aes-1 AES 키 도착 전 체결통보 버퍼링 없이 유실(콜드스타트, reconcile 백스톱)
- frag-1 프레임 재조립 상태오류로 부분메시지 폐기(KIS 미분할이라 도달성 낮음)
- handshake-1 Sec-WebSocket-Accept 불일치 거부 안 함(경고만)
- midnight-1 approval_key/AES 자정 회전 부재(불확실 — KIS TTL 동작 미확인)
- ctrlframe-1 제어프레임 크기 미검증(대형 ping 에코 증폭)
- exec-count-1 체결통보 다중레코드 정렬 휴리스틱 위양성(하류 방어 있음)

**auth.js + order-journal.js (7, LOW/불확실)**
- journal-3/4 reconcile falsy userId·누락종목 처리(주 경로 안전, 레거시 _global만 잠재)
- auth-2 decrypt GCM 실패를 빈문자열로 삼킴(무음 자격증명 소실)
- journal-5/6/8 markFilled 가격밴드 과대·중복 이중가산·음수수량 미검(표시 한정, reconcile 교정, VTS 비활성)
- auth-4 콜드스타트 타이밍 누설로 유저 존재 오라클(악용 창 극히 좁음, 불확실)
- journal-7 시장가 주문 접수 즉시 '체결' 낙관 확정(의도적 트레이드오프)

**app.html 기능부 (4, LOW)**
- misc-1 txMode 정규화가 안전기본값 역전(unknown→demo). 계약외 값/다기기 전환 시만 재현. 안전 방향은 unknown→null(모드 미확인 경고)
- misc-2 ord_dvsn_name이 esc() 없이 innerHTML(규칙 위반, 현재 KIS 고정라벨이라 실익 낮음)
- misc-4 로그아웃 시 aiHistory·lastStock·priceAlerts 등 uk 스코프 키 잔존(공용PC 위생)
- misc-5 setText에 `<span>` 마크업 전달 → 태그 그대로 노출(장중 상태 표시 깨짐)

**app.html 차트 렌더링 (9, LOW 8 + render-4는 위 MEDIUM)**
- render-1 parseChart가 open/high/low 0·NaN 미검 → 스케일 붕괴(데이터 이상 시)
- render-2 볼린저밴드가 min/max 범위 계산에서 빠져 플롯 밖으로 넘침
- render-3 renderChart에 W/H==0 가드 없음(숨김 상태 resize 시 0폭 캔버스)
- render-5 _applyChartData가 dashHover 미리셋(mouseleave로 대개 방어, 불확실)
- render-6 줌인 시 n<=p 가드로 MA선 사라짐(데이터 충분한데도)
- render-7 메인 캔들차트 터치 이벤트 없음 → 모바일 팬/줌/호버 불가
- render-8 가격 무변동(min==max) 시 캔들이 하단축에 눌림
- render-9 서브차트 offsetWidth 불일치·0폭 미방어(경미)
- render-10 호버 중 매 mousemove마다 전체 renderChart(성능·깜빡임)

**app.html 차트 지표 수학 (3, LOW 3 + macd-1은 위 HIGH)**
- macd-2 MACD 데이터 충분성 가드가 27~34봉에서 빈 패널 허용(신생종목 엣지)
- rsi-1 RSI 완전평탄 시 차트=100 vs 엔진=50 불일치(거래정지 등 드묾)
- rsi-2 RSI가 Cutler(단순평균) 방식 — HTS(Wilder)와 수치 차이(설계 선택, 차트·봇은 상호 일관)

---

## 권장 처리 순서

1. **pos-1** (실제 돈, 핵심 안전장치 붕괴) — 최우선
2. **macd-1** ("모든 차트" 직결, 한 글자 수정) — 즉시
3. **reconcile-1 · loss-1** (무손절·서킷 오염, 자동매매 신뢰성)
4. **watchdog-1 · render-4** (시세 동결·종목 오표시, 사용자 체감)
5. lru-1 · reconnect-1 (다중 사용자 출시 전)
6. 나머지 LOW는 배치로 일괄
